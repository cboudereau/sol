# CLAUDE.md

## Active workspaces
- [parquet-backend](docs/workspace/parquet-backend/TASKS.md) — read-side query backend (Prometheus/Tempo/Loki + SQL APIs over Parquet via DataFusion) incl. compaction, rollups, telemetry, and end-to-end demo integration. Phase 5 — ✅ **Sessions 1–6 complete** (checkpoint green: query:: 74/74, clippy -D warnings clean). S1: catalog logs/traces/metrics(union) over DataFusion (14b deferred — codec per-subtype dirs + sort). S2: Task 3 (LogQL→SQL + Loki query_range), Task 4 (PromQL instant `/prometheus/api/v1/query` GET+POST, `/label/:name/values`, `/series`). S3: Task 5 (PromQL range → `/query_range`: rate via LAG+counter-reset, `<agg>_over_time` via RANGE frame, sum-by, topk/bottomk; matrix response; 2 end-to-end execution tests), Task 6 (`histogram_quantile` — Rust-native over OTLP bucket_counts/explicit_bounds per rabbit-hole #5: linear interp, +Inf→last bound, empty→None). JSON extraction via **datafusion-functions-json 0.53.1** (`register_all`; replaced custom UDF — [ADR](docs/workspace/parquet-backend/adrs/json-attribute-extraction.md); pinned set = datafusion/datafusion-functions-json/object_store/promql-parser). Shared `src/query/routes.rs` make_routes. S4: Task 7 (Tempo/TraceQL — `/api/search`, `/api/v2|v1/traces/:id`, tags, tag values; `arrow_cast(X'..','FixedSizeBinary(16)')` trace lookup, `encode(_,'hex')`), Task 8 (query cache — **moka** 0.12, `CacheKey` 15s-bucketed, `QueryCache` trait for future Redis; wired into `QueryEngine.sql`, invalidated on `refresh()`), Task 9 (querier telemetry — `src/query/telemetry.rs` emits `sol_query_*`/`sol_objectstore_*` via `metrics` macros, matches dashboard; `record_cache` wired). Pinned set += moka. S5: Task 10 (standalone compactor — `src/query/compaction.rs`: sealed-day Parquet→Parquet sort-merge, footer `level`/`supersedes`/`resolution` provenance, atomic staging→rename, `resolve_files` supersession-skip, retention GC; `sol_compactor_*`), Task 11 (query-frontend — `src/query/frontend.rs`: midnight-aligned `split`, `merge_series`/`merge_topk`/`merge_histogram_quantile`, permanent historical-shard cache; `sol_query_shard_*`). S6: Task 12 (metric rollup tiers — `src/query/rollup.rs`: `RollupTier`, `select_tier`, `rollup_batches` last-point-per-bucket, `generate_rollup`; metrics-only), Task 13 (cross-signal SQL — `src/query/sql.rs`: `POST /api/v1/sql`, NFR9 scan guardrail → 422, `logs⨝traces` on `trace_id`, `metrics⨝traces` on service+time-window, arrow-JSON results). S7: Task 15 (demo integration — `sol-query` compose service + `sol-query.yaml` (validated via `sol validate` ✅), parallel `Sol-Prometheus/Tempo/Loki` datasources, all 4 dashboards on a `$datasource` variable). **All code tasks complete (Sessions 1–7).** Pending: (a) task 14b DEFERRED — codec per-subtype dirs + sort, a write-side codec API change, **outside autopilot constitution → needs your decision**; (b) task 15 live-stack verification (Grafana Save&Test, Sol↔Mimir parity, NFR6 latency) needs `docker compose --profile parquet up` (not runnable here). **Phase 6 (integrate ADRs/DESIGN to durable docs, delete workspace) awaiting go-ahead.**
