# CLAUDE.md

## Active workspaces
- [backend-metrics-perf](docs/workspace/backend-metrics-perf/TASKS.md) — **Phase 5 autopilot, task 0/9 (S1: 1,1b,2,3; S2: 6,4,5; S3: 7,8).** 3 ADRs ACCEPTED (file-pruning A′ = exact-bounds file names + store wipe; cache B+D+F; guardrail semaphore). Querier per-query fixed-cost elimination. Live-measured on the demo (session backend-metrics-perf): every query pays ~0.25 s opening ALL ~1,529 Parquet footers for execution-time stats pruning (EXPLAIN ANALYZE: files_ranges_pruned_statistics=1.35K/query; flat latency 5m→3h; nonexistent metric 0.27 s); RED dashboard = 20 concurrent queries → ~2.3 s wall @ ~9.7 cores vs Mimir 9 ms; cache wiped every 15 s by refresh() clear(); no single-flight; metadata endpoints default start=0; guardrails.max_concurrent_queries parsed but never enforced (dead config). Scope: time-pruned per-query file listing, scoped cache invalidation, request coalescing, bounded metadata, enforce-or-remove max_concurrent_queries. NOT started implementing.
  RESUME: load the `my-plan` skill, then read TASKS.md (checked = done) + `git log --oneline`; continue at first unchecked task; re-run the session checkpoint before trusting state.
- [rollup-read-routing](docs/workspace/rollup-read-routing/TASKS.md) — **Phase 5 autopilot COMPLETE (all 7 tasks, S1-S3 committed); Phase 6 integration pending live-demo verification.** ADRs accepted. S1 `cab4128d3` (rich rollup + capability choke point), S2 `74baff9ce` (range+histogram routed; select_range_table+tiered_hist_source deleted; magic ns→DurationNs::DAY), S3 `b6a81b3b5` (instant+metadata routed; no-bypass tier guard). querier:: 206 passed, 0 failed, 1 ignored; clippy clean. Result: every metric path (range/histogram/instant/metadata) routes through one `resolve_metric_windows` choke point + `op_capability` (Last/MinMax/SumCount/None); rich rollup carries per-bucket {min,max,sum,count} so max/min/avg/sum/count_over_time use a tier AND match raw. Post-impl: review fixes `bea64417d` (dedupe tier mapping + min/sum/count parity tests). **Live parity (Sol↔Mimir on the demo) caught + fixed TWO instant-path regressions**: (1) `9e60ac764` — omitted-`time` instant range-fns (i64::MAX sentinel) built a year-2262 window → empty; now anchored to wall-clock now (`instant_anchor`). (2) `d8d25be16` — instant `rate`/`increase` returned ~½ (Sol range==Mimir==25.7, instant==13): the `[anchor−range, anchor]` scan lacked the LAG predecessor for the window's leading sample → dropped one leading delta; now `instant_range_windows` extends the lower bound by an extra `range` for LAG ops (`is_lag_range_op`) to seed it, frame width unchanged. (3) `d1305ef82` — instant `sum(rate(m[w]))` dropped offset-scraped series: `lower_instant_aggregate_range` grouped by `(group_key, time)` then picked global-latest timestamp → kept only series sampled on that exact ts; now collapses per-series-first (latest_per_series by series identity, no time) then aggregates. 4-case instant==range parity matrix added (`offset_multiseries_engine`). querier:: 212 green. **Verified exact Sol↔Mimir parity for gauges/max/avg/count + instant per-series rate** (earlier "2× gauge gap" was Mimir staleness retaining pre-rebuild `instance` UUIDs across restart, NOT a Sol bug — host_name is 1:1 with the live instance). **Open, OUT OF SCOPE (pre-existing range-rate algorithm, not rollup-read-routing): RANGE rate graph zigzags vs Mimir** — raw counter is monotonic-cumulative & identical to Mimir (NOT reset/temporality); two mechanisms: (a) left-edge ramp = range scan `[start,end]` lacks pre-window lookback (same class as the instant LAG fix — mirror it into `handle_range`), (b) throughout bias+jitter = Sol non-extrapolating rate vs Mimir extrapolation (esp. `[2m]` jitter 1.93 vs 0.22). Needs its own workspace. Also pre-existing: instant `histogram_quantile` dispatch errors (range works). **Perf verified live + fixed `bfc273f91`**: tier scans 15–54× fewer rows & ~6× faster cold (direct SQL, sealed day: tier 268k/88k/74k vs raw 4.03M rows; 90ms vs 533ms) — tier values correct (max/min/histogram exact vs Mimir). But routing under-used the tier: `sealed_ns` was `end_ns−1day` (query-end-relative) so historical-end queries read raw; now `now_ns−1day` (wall-clock), threaded from routes — historical+live coarse queries now hit the tier (kept the safe 24h margin; day-alignment to capture the last sealed day is a deferred follow-up needing a tier-coverage cap). **Open/deferred (NOT blocking, separate from this workspace): (a) avg/sum/count_over_time over a tier are exact only for bucket-aligned windows — ≤1-bucket edge approximation otherwise (ADR "exact" claim should be qualified); (b) day-aligned sealed boundary; (c) RANGE-rate graph zigzag (non-extrapolating rate + no pre-window lookback); (d) instant histogram_quantile dispatch.** **Pending: USER REBUILD of the image, then re-run the broader Sol↔Mimir parity sweep (instant/range × rate/max/avg/histogram/metadata); then 7-day CPU once days seal + rollups regenerate; then Phase 6 (integrate ADRs/DESIGN→durable docs/<date>_rollup-read-routing/, delete workspace, remove this entry).** Demo store was wiped (clean cutover) — only the active day, 0 rollups yet, so tier path can't be exercised live until a day seals. Goal met: "always use the best tier that can answer correctly" — one tier-resolution choke point for every path (range-agg, range-histogram, instant, instant-histogram, metadata) + an **operator→capability** model (`Last`/`MinMax`/`SumCount`/`None`) routing to the coarsest tier carrying the capability, instead of step-only + force-raw. **Scope now includes the write side** (per "why separate??" — splitting read/write would re-create the very gap that caused this): FR6 enriches the rollup to carry per-bucket `{last,min,max,sum,count}` (new nullable cols on the shared metric schema, clean cutover) so max/min/avg/sum/count_over_time route to a tier **and** stay exact; FR7 selects the per-op aggregate column on read. Irreducible raw: irate, quantile/stddev/stdvar_over_time, active day. Closes the write-ahead-of-read gap surfaced live (225% querier CPU on 7-day metrics; histogram range fixed point-wise in `40149d8fa`; instant+metadata still raw; routing operator-unaware → `max_over_time` silently drops peaks on 8+ panels). 7 tasks / 3 sessions. **4 ADRs `proposed`** (choke-point, operator→capability+rich-rollup, rollup-aggregate-schema, instant&metadata). Earlier review banked: baseline querier:: 177 green @ `20e203c51`; catalog refresh profiled & ruled out (~30 ms/15 s) → non-goal; raw vs tier scan ratio measured (5m=9%/~11×, 1h=3.3%/~30×, 1d=2.7%/~37×).
  RESUME: load the `my-plan` skill, then read TASKS.md (checked = done) + `git log --oneline`; continue at first unchecked task; re-run the session checkpoint before trusting state.
- [range-rate-parity](docs/workspace/range-rate-parity/TASKS.md) — **Phase 5 COMPLETE (S1, all 3 tasks, `2d07c34e2`); Phase 6 pending live-demo verification.** ADR accepted. querier:: 220 passed, 0 failed, 1 ignored; clippy clean. rate()/increase() now use Prometheus extrapolatedRate (frame.rs — first_value_udwf/MAX/COUNT/SUM/FIRST_VALUE over the RANGE frame; base=sum_delta−first_delta; edge extrapolation ≤avg_gap/2 + counter zero-clamp; cnt<2→NULL; irate/over_time untouched); handle_range wires frontend shard lookback (matrix_range_ns, scan from query_start_ns, emit only [shard.start,shard.end]) → no left-edge ramp / day-boundary dip. Golden test recovers true slope 6.6667/s within 1e-6; 4 instant==range parity tests held. **Pending: USER REBUILD → confirm the RED-dashboard rate graph is smooth (matches Mimir, zigzag gone); then Phase 6 (integrate → docs/<date>_range-rate-parity/, delete workspace, remove this entry).** Original planning context: Fix the range `rate()`/`increase()` Sol↔Mimir parity: the RED-dashboard graph zigzags on Sol (smooth on Mimir). Root cause (from `plan/frame.rs::rate`): windowed increase = SUM of per-sample reset-adjusted deltas over the half-open `(t−range,t]` RANGE frame (telescopes to `v_last_in_window − v_last_before_window`), divided by the **fixed** `range_ns`; as the eval grid slides, samples cross the trailing edge so the increase-span oscillates ~`range`..`range+scrape` while the divisor is fixed → rate oscillates ±(scrape/range). Prometheus **extrapolates** to the window boundaries → smooth. Also a left-edge ramp (range scan `[start,end]` lacks pre-window lookback). Data confirmed monotonic-cumulative & identical to Mimir (NOT reset/temporality). Pre-existing (predates rollup-read-routing). Scope: Prometheus-compatible extrapolation for rate/increase(/irate) + pre-window lookback; keep instant==range parity. Non-goals: day-aligned sealed boundary, instant histogram_quantile dispatch, avg/sum tier exactness (separate).
  RESUME: load the `my-plan` skill, then read TASKS.md (checked = done) + `git log --oneline`; continue at first unchecked task; re-run the session checkpoint before trusting state.
- [parquet-backend](docs/workspace/parquet-backend/TASKS.md) — read-side query backend (Prometheus/Tempo/Loki + SQL APIs over Parquet via DataFusion) incl. compaction, rollups, telemetry, and end-to-end demo integration. Phase 5 — ✅ **Sessions 1–6 complete** (checkpoint green: querier:: 74/74, clippy -D warnings clean). S1: catalog logs/traces/metrics(union) over DataFusion (14b deferred — codec per-subtype dirs + sort). S2: Task 3 (LogQL→SQL + Loki query_range), Task 4 (PromQL instant `/prometheus/api/v1/query` GET+POST, `/label/:name/values`, `/series`). S3: Task 5 (PromQL range → `/query_range`: rate via LAG+counter-reset, `<agg>_over_time` via RANGE frame, sum-by, topk/bottomk; matrix response; 2 end-to-end execution tests), Task 6 (`histogram_quantile` — Rust-native over OTLP bucket_counts/explicit_bounds per rabbit-hole #5: linear interp, +Inf→last bound, empty→None). JSON extraction via **datafusion-functions-json 0.53.1** (`register_all`; replaced custom UDF — [ADR](docs/workspace/parquet-backend/adrs/querier/json-attribute-extraction.md); pinned set = datafusion/datafusion-functions-json/object_store/promql-parser). Shared `src/querier/routes.rs` make_routes. S4: Task 7 (Tempo/TraceQL — `/api/search`, `/api/v2|v1/traces/:id`, tags, tag values; `arrow_cast(X'..','FixedSizeBinary(16)')` trace lookup, `encode(_,'hex')`), Task 8 (query cache — **moka** 0.12, `CacheKey` 15s-bucketed, `QueryCache` trait for future Redis; wired into `QueryEngine.sql`, invalidated on `refresh()`), Task 9 (querier telemetry — `src/querier/telemetry.rs` emits `sol_querier_*`/`sol_objectstore_*` via `metrics` macros, matches dashboard; `record_cache` wired). Pinned set += moka. S5: Task 10 (standalone compactor — `src/querier/compaction.rs`: sealed-day Parquet→Parquet sort-merge, footer `level`/`supersedes`/`resolution` provenance, atomic staging→rename, `resolve_files` supersession-skip, retention GC; `sol_compactor_*`), Task 11 (query-frontend — `src/querier/frontend.rs`: midnight-aligned `split`, `merge_series`/`merge_topk`/`merge_histogram_quantile`, permanent historical-shard cache; `sol_querier_shard_*`). S6: Task 12 (metric rollup tiers — `src/querier/rollup.rs`: `RollupTier`, `select_tier`, `rollup_batches` last-point-per-bucket, `generate_rollup`; metrics-only), Task 13 (cross-signal SQL — `src/querier/sql.rs`: `POST /api/v1/sql`, NFR9 scan guardrail → 422, `logs⨝traces` on `trace_id`, `metrics⨝traces` on service+time-window, arrow-JSON results). S7: Task 15 (demo integration — `sol-querier` compose service + `sol-querier.yaml` (validated via `sol validate` ✅), parallel `Sol-Prometheus/Tempo/Loki` datasources, all 4 dashboards on a `$datasource` variable). **All code tasks complete (Sessions 1–7).** ✅ **Task 14b done** (codec sorts each Parquet by service_name/name/time on write; gateway `route` transform → per-subtype `metrics/<subtype>/dt=…` sinks). ✅ **Full runtime role-wiring done** (the components were library-only before): (A) supersession-aware reads — catalog registers each table over a recursively-walked + `resolve_files`-curated explicit file list (finds nested per-subtype dirs, skips superseded raw, no double-count); (B) **compactor daemon** — `query.role: querier|compactor` + periodic seal→rollup→GC loop (`Compactor::run_once`) + `sol-compactor` singleton demo service (rw); (C) **query-frontend** — `handle_range` splits long ranges into per-day shards + merges; (D) **rollup tier tables** — `metrics_5m/1h/1d` registered over `rollup-<tier>.parquet`, range queries routed to the coarsest tier ≤ `step` via `select_range_table`. Tempo API now under `/tempo`. Pending: task 15 live-stack verification (Grafana Save&Test, Sol↔Mimir parity, NFR6 latency) needs `docker compose --profile parquet up` (not runnable here). **Phase 6 (integrate ADRs/DESIGN to durable docs, delete workspace) awaiting go-ahead.**
