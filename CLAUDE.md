# CLAUDE.md

## Standing directives
- **No parquet/rollup retro-compat, ever**: any storage layout/schema change ships as a clean cutover (demo store wipe); never write dual-format read paths or migration code.

## Active workspaces
- [write-side-small-files](docs/workspace/write-side-small-files/TASKS.md) — **Phase 4 complete, awaiting ADR ratification.** Lever 1 from docs/20260717_promql-plan-cache/README.md "Next levers": live 15-min metrics windows contain ~240 files (files_opened p95 = 237; gateway flushes ~14 files/min across sinks) → bare-range floor 304 ms, execute-dominated rate() 370–420 ms. PREMISE CORRECTED by exploration: closed-hour compaction already ships & is on in the demo (compact_active_day, intraday:true) — the ~240 files are the OPEN HOUR's raw tail. Scope: sub-hour chunked write-once compaction of the open hour (exact-bounds-named level-1 outputs → zero querier parser changes, no wipe), demo compactor tick 300→60 s; flush cadence rejected as primary lever (freshness). ADR open-hour-chunk-compaction PROPOSED (A: chunked ~2× write amp vs B rolling ~30× vs C cadence freshness-regressing). Plan committed, awaiting ratification + go. Row-work levers (E / series-key) stay separately owned by the promql-plan-cache follow-up note.
  RESUME: load the `my-plan` skill, then read TASKS.md (checked = done) + `git log --oneline`; continue at first unchecked task; re-run the session checkpoint before trusting state.