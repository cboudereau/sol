# CLAUDE.md

## Standing directives
- **No parquet/rollup retro-compat, ever**: any storage layout/schema change ships as a clean cutover (demo store wipe); never write dual-format read paths or migration code.

## Active workspaces
- [rate-row-work](docs/workspace/rate-row-work/TASKS.md) — **Phase 5: S1 DONE (T1 frame reduction 6→5 windows; T2 re-profile: fixture rate() 47ms cold/27ms warm ≤80ms → FR2/FR3 DEFERRED, execute cut ~7×). S2 REOPENED (FR1 live missed NFR1 ~300ms: execute is per-row prom_series_key UDF + window sort, not per-window). Running T3 (stored series-key column) → T4 (write-sort pushdown); then S3 live needs rebuild + STORE WIPE (schema cutover).** Final follow-up of the querier-perf arc (after docs/20260717_promql-plan-cache + docs/20260720_write-side-small-files): `rate()`/`increase()` are execution-bound (live stage means execute ~835 ms / physical ~122 ms; plan cache already eliminated the optimize stage). Two levers: (E) shrink the frame.rs rate() lowering (fuse/drop window aggregates, gated by the extrapolation golden test + instant==range parity matrix), and (series-key) materialise prom_series_key as a stored metric-schema column (clean-cutover + store wipe per standing directive) so window PARTITION BY drops the per-row UDF and can use sort-pushdown. Owns NFR1 (cold repeated-shape rate() ≤ 80 ms) + NFR2 (burst ≤ 0.5 s), inherited twice. 2 ADRs ACCEPTED 2026-07-20 (rate-frame-reduction: fuse/drop the 6 rate() windows to ~3, golden-gated, no wipe; series-key-column: stored col + write-sort pushdown, clean cutover + wipe, GATED on FR1 re-profile). Sequenced FR1→re-profile→(FR2+FR3 only if FR1 misses ≤80 ms). Baseline expected querier:: 261/0/2.
  RESUME: load the `my-plan` skill, then read TASKS.md (checked = done) + `git log --oneline`; continue at first unchecked task; re-run the session checkpoint before trusting state.