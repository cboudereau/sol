# CLAUDE.md

## Active workspaces
- [parquet-backend](docs/workspace/parquet-backend/TASKS.md) — read-side query backend (Prometheus/Tempo/Loki + SQL APIs over Parquet via DataFusion) incl. compaction, rollups, telemetry, and end-to-end demo integration. Phase 5 — ✅ **Sessions 1 & 2 complete** (checkpoint green: query:: 21/21, clippy -D warnings clean). S1: catalog logs/traces/metrics(union) over DataFusion (14b deferred — codec per-subtype dirs + sort). S2: Task 3 (LogQL→SQL + Loki query_range), Task 4 (PromQL instant `/prometheus/api/v1/query` GET+POST, `/label/:name/values`, `/series` via promql-parser; `op.id()` agg, latest-per-series window, vector response). JSON extraction now via **datafusion-functions-json 0.53.1** (`register_all`; replaced the custom UDF — [ADR](docs/workspace/parquet-backend/adrs/json-attribute-extraction.md); pinned set = datafusion/datafusion-functions-json/object_store/promql-parser). Shared `src/query/routes.rs` make_routes (tasks 5/7/13 add filters there). **Next: Session 3** — task 5 (PromQL range: rate/sum-by/topk/max_over_time → `/query_range`) then task 6.
